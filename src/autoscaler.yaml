

apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: php-backend
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: php-backend

  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80

